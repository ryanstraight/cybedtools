% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-invariants.R
\name{assert_graph_invariants}
\alias{assert_graph_invariants}
\title{Check an assembled graph against the declared \code{graph_invariants} bands}
\usage{
assert_graph_invariants(rdf, invariants_path = NULL, declared = NULL)
}
\arguments{
\item{rdf}{An rdf object.}

\item{invariants_path}{Character path to the invariants file. Defaults to
\code{docs/framework-invariants.yml} in the source checkout.}

\item{declared}{Optional pre-read \code{graph_invariants} list, used in place of
reading the file. Mainly for tests.}
}
\value{
Invisibly, the tibble of measured counts.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Compares \code{\link[=graph_invariant_counts]{graph_invariant_counts()}} against the bands declared in the
\code{graph_invariants} block of \code{docs/framework-invariants.yml}. Each band is a
pair of inclusive bounds; a pair with two equal bounds is an exact value.
A measured quantity outside its band raises a classed condition naming
every disagreement.

Bands are the file's claim about the pipeline's output. A value that leaves
its band is a human-review event: something upstream moved, and the right
response is to find out what before editing the number.
}
\examples{
\dontrun{
assert_graph_invariants(load_combined_ntriples_graph())
}
}
\seealso{
Other Graph invariants:
\code{\link[=assert_graph_identity]{assert_graph_identity()}},
\code{\link[=graph_identity_violations]{graph_identity_violations()}},
\code{\link[=graph_invariant_counts]{graph_invariant_counts()}}
}
\concept{Graph invariants}
