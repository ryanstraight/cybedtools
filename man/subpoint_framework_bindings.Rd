% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparql-helpers.R
\name{subpoint_framework_bindings}
\alias{subpoint_framework_bindings}
\title{Domain helper: subpoint-to-framework bindings with framework name attached}
\usage{
subpoint_framework_bindings(rdf)
}
\arguments{
\item{rdf}{An rdf object.}
}
\value{
A tibble with columns \code{subpoint}, \code{framework}, \code{framework_name},
\code{framework_slug} (added v0.4.0, a stable join key).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

One row per (subpoint, framework) pair where the subpoint is typed
\code{cybed:Subpoint} (the generic enumeration-list-splitting subtype --
"such as X, Y, and Z" / "including A and B" -- parsed out of a single
native unit's text at JSON-LD assembly time, applied uniformly across
the corpus) and its \code{partOf} target is typed
\code{cybed:Framework}. Subpoints without a valid framework partOf are
excluded.

Subpoints are a strict subset of the elements returned by
\code{\link[=element_framework_bindings]{element_framework_bindings()}}, and a distinct subtype from
\code{\link[=example_framework_bindings]{example_framework_bindings()}}'s \code{cybed:Example} (the Cyber.org K-12 /
CSTA Clarification-statement pedagogical-scaffolding subtype
specifically). Use this helper together with
\code{\link[=example_framework_bindings]{example_framework_bindings()}} when constructing a "strict" native
element count by subtracting both Subpoint and Example counts from the
total element count -- see \code{framework_summary}'s \code{element_count_strict}
column, which does exactly this.
}
\note{
The \code{framework} column holds the framework's full URI. Avoid
naming a local variable or function parameter \code{framework} in code
that filters or mutates this tibble -- dplyr's data masking silently
resolves a bare \code{framework} reference inside \code{filter()}/\code{mutate()}
to this COLUMN rather than your same-named variable, with no error
and no warning, only a wrong (often zero-row) result surfacing later.
}
\examples{
\dontrun{
rdf <- load_combined_ntriples_graph()
subpoint_framework_bindings(rdf)
}
}
\seealso{
Other SPARQL helpers:
\code{\link[=element_framework_bindings]{element_framework_bindings()}},
\code{\link[=element_text]{element_text()}},
\code{\link[=example_framework_bindings]{example_framework_bindings()}},
\code{\link[=framework_metadata]{framework_metadata()}},
\code{\link[=organizing_unit_framework_bindings]{organizing_unit_framework_bindings()}},
\code{\link[=role_element_bindings]{role_element_bindings()}},
\code{\link[=role_framework_bindings]{role_framework_bindings()}},
\code{\link[=sparql_pairs]{sparql_pairs()}},
\code{\link[=sparql_subjects]{sparql_subjects()}},
\code{\link[=unit_element_bindings]{unit_element_bindings()}},
\code{\link[=unit_relation_bindings]{unit_relation_bindings()}}
}
\concept{SPARQL helpers}
