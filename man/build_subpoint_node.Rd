% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsonld-helpers.R
\name{build_subpoint_node}
\alias{build_subpoint_node}
\title{Construct a \code{cybed:Subpoint} node}
\usage{
build_subpoint_node(
  parent_element_id,
  ordinal,
  text,
  framework_prefix,
  framework_id,
  parent_subtype = "RoleElement"
)
}
\arguments{
\item{parent_element_id}{Character, the framework-local id of the parent
element (without prefix), e.g., \code{"K-2.SEC.AUTH"}.}

\item{ordinal}{Integer, the 1-based subpoint ordinal within the parent.}

\item{text}{Character, the subpoint's text fragment.}

\item{framework_prefix}{Character, Tier 2 prefix.}

\item{framework_id}{Character, framework identifier for \code{cybed:partOf}.}

\item{parent_subtype}{Character, the parent element's framework subtype
(e.g., \code{"Standard"}, \code{"SkillLevel"}). The subpoint is also typed as
this subtype so it appears in framework-native queries.}
}
\value{
Named list (JSON-LD node) for the subpoint.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

A subpoint is a granular pedagogical or specification fragment lifted out
of a parent element's prose (typically from a "such as", "examples of",
or semicolon-delimited list inside \code{cybed:elementText}). Subpoints carry
the framework-native subtype \code{cybed:Subpoint} plus the parent's framework
subtype, retain \code{cybed:partOf} to the cluster, and link back to the
parent element via \code{cybed:elaborates}.

Use \code{\link[=parse_subpoints]{parse_subpoints()}} to derive subpoint records from a parent's text;
this constructor turns one such record into a JSON-LD node.
}
\examples{
sp <- build_subpoint_node(
  parent_element_id = "K-2.SEC.AUTH",
  ordinal           = 1,
  text              = "not using common words as passwords",
  framework_prefix  = "cyberorg",
  framework_id      = "cyberorg-k12-v1.0",
  parent_subtype    = "Standard"
)
sp[["@id"]]
sp[["cybed:elaborates"]]
}
\seealso{
Other Sub-point parsing:
\code{\link[=expand_with_subpoints]{expand_with_subpoints()}},
\code{\link[=extend_role_element_ids]{extend_role_element_ids()}},
\code{\link[=parse_subpoints]{parse_subpoints()}}
}
\concept{Sub-point parsing}
