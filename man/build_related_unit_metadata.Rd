% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsonld-helpers.R
\name{build_related_unit_metadata}
\alias{build_related_unit_metadata}
\title{Build \code{cybed:relatedUnit} metadata for an organizing unit}
\usage{
build_related_unit_metadata(to_unit_ids, to_prefix)
}
\arguments{
\item{to_unit_ids}{Character vector of target unit identifiers.}

\item{to_prefix}{Character, Tier 2 prefix the targets live under: one
value for all targets, or one per target. Differs from the source
unit's prefix when the relation crosses frameworks.}
}
\value{
Named list with one entry, \code{cybed:relatedUnit}, or an empty list
when there are no targets.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Some frameworks relate one organizing unit to another: a job role to the
skills it requires, a knowledge unit to the outcomes it supports. The
plain \code{cybed:relatedUnit} edge records that the publisher relates the two
units and nothing more, so "which units does this unit point at" stays a
one-hop query. When the publisher qualifies the relation (a required
proficiency level, a named relation type), also emit a
\code{\link[=build_unit_relation_node]{build_unit_relation_node()}} for the pair.

Pass the result as (part of) the \code{metadata} argument of
\code{\link[=build_organizing_unit_node]{build_organizing_unit_node()}} or \code{\link[=build_role_node]{build_role_node()}}. Call it ONCE per
source unit with every target that unit has: two calls merged with \code{c()}
give the node two \code{cybed:relatedUnit} keys, which is not valid JSON-LD.
Targets in several frameworks go in one call, with \code{to_prefix} given per
target.
}
\examples{
build_related_unit_metadata(c("skill-a", "skill-b"), "otccf")

# Targets in two frameworks, one call.
build_related_unit_metadata(c("skill-a", "OG-WRL-015"), c("otccf", "nice"))
}
\seealso{
Other JSON-LD construction:
\code{\link[=assemble_framework_document]{assemble_framework_document()}},
\code{\link[=build_framework_node]{build_framework_node()}},
\code{\link[=build_jsonld_context]{build_jsonld_context()}},
\code{\link[=build_multi_framework_context]{build_multi_framework_context()}},
\code{\link[=build_organizing_unit_node]{build_organizing_unit_node()}},
\code{\link[=build_role_element_node]{build_role_element_node()}},
\code{\link[=build_role_node]{build_role_node()}},
\code{\link[=build_unit_relation_node]{build_unit_relation_node()}}
}
\concept{JSON-LD construction}
