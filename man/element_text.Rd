% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparql-helpers.R
\name{element_text}
\alias{element_text}
\title{Domain helper: statement text keyed by element}
\usage{
element_text(rdf)
}
\arguments{
\item{rdf}{An rdf object.}
}
\value{
A tibble with columns \code{element} (character, the element's full
URI) and \code{text} (character, the statement literal).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

One row per element that carries a \code{cybed:elementText} literal, from
the single basic graph pattern \code{sparql_pairs(rdf, "cybed:elementText")}
renamed from \code{s}/\code{o}. This is the statement text itself -- the task,
knowledge, skill, competency, or standard wording as the framework
publishes it.
}
\details{
Nothing is filtered, normalised, or de-duplicated beyond what the graph
holds. Elements without a \code{cybed:elementText} triple simply do not
appear, and an element carrying more than one \code{cybed:elementText}
literal yields one row per literal, so \code{element} is not guaranteed
unique. A graph with no \code{cybed:elementText} triples at all returns a
zero-row tibble with the same two columns.

Sub-points and examples are elements too, and they carry their own
text: a \code{cybed:Subpoint} holds the enumerated fragment lifted out of
its parent's wording, and a \code{cybed:Example} holds the pedagogical
"Clarification statement:" content. A caller who wants parent
statements only should anti-join both child sets away, matching each
helper's own identifier column against \code{element}:
\code{\link[=subpoint_framework_bindings]{subpoint_framework_bindings()}} returns \code{subpoint}, and
\code{\link[=example_framework_bindings]{example_framework_bindings()}} returns \code{example}.
}
\examples{
rdf <- make_demo_graph()
# The demo graph carries no cybed:elementText, so this is a zero-row
# tibble with the columns `element` and `text`.
element_text(rdf)

\dontrun{
rdf <- load_combined_ntriples_graph()
texts <- element_text(rdf)

# Parent statements only: drop sub-points and examples.
texts |>
  dplyr::anti_join(
    dplyr::rename(subpoint_framework_bindings(rdf), element = "subpoint"),
    by = "element"
  ) |>
  dplyr::anti_join(
    dplyr::rename(example_framework_bindings(rdf), element = "example"),
    by = "element"
  )
}
}
\seealso{
\code{\link[=element_framework_bindings]{element_framework_bindings()}} to attach framework
attribution, \code{\link[=subpoint_framework_bindings]{subpoint_framework_bindings()}} and
\code{\link[=example_framework_bindings]{example_framework_bindings()}} to separate child elements from
parent statements.

Other SPARQL helpers:
\code{\link[=element_framework_bindings]{element_framework_bindings()}},
\code{\link[=example_framework_bindings]{example_framework_bindings()}},
\code{\link[=framework_metadata]{framework_metadata()}},
\code{\link[=organizing_unit_framework_bindings]{organizing_unit_framework_bindings()}},
\code{\link[=role_element_bindings]{role_element_bindings()}},
\code{\link[=role_framework_bindings]{role_framework_bindings()}},
\code{\link[=sparql_pairs]{sparql_pairs()}},
\code{\link[=sparql_subjects]{sparql_subjects()}},
\code{\link[=subpoint_framework_bindings]{subpoint_framework_bindings()}},
\code{\link[=unit_element_bindings]{unit_element_bindings()}},
\code{\link[=unit_relation_bindings]{unit_relation_bindings()}}
}
\concept{SPARQL helpers}
