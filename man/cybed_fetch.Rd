% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cybed-fetch.R
\name{cybed_fetch}
\alias{cybed_fetch}
\title{Download and hash-verify per-framework release files into the user cache}
\usage{
cybed_fetch(frameworks = NULL, version = NULL)
}
\arguments{
\item{frameworks}{Character vector of framework slugs to fetch (as
carried by \link{framework_summary}\verb{$framework_slug}, e.g. \code{"nice-v2"}, or the release file slug, e.g. \code{"nice"}), or
\code{NULL} (the default) for every framework the release manifest ships.}

\item{version}{Character scalar release version (e.g. \code{"1.0.0"}), or
\code{NULL} (the default) for the data release this package version was built against, \code{cybed_data_release}.}
}
\value{
Invisibly, a tibble with one row per fetched framework: columns
\code{framework_slug}, \code{path} (the cached file's local path),
\code{sha256_verified} (logical, always \code{TRUE} on return -- a mismatch
aborts instead of returning \code{FALSE}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Downloads the release manifest and the named frameworks' \code{.nt.gz} files
from the configured release location (see the release URL resolution rules documented below),
verifies each file's SHA-256 against the hash the manifest declares, and
writes only into \code{tools::R_user_dir("cybedtools", "cache")} -- never any
other location on disk, per CRAN policy. A file whose downloaded bytes
do not match its manifest hash is deleted and the call aborts with class
\code{cybedtools_hash_mismatch} rather than leaving a corrupt cache entry.

Already-cached files whose hash still matches the manifest are not
re-downloaded.
}
\examples{
\donttest{
# Requires network access (or a `cybedtools.release_url` override
# pointing at a local mock release for testing).
tryCatch(
  cybed_fetch(frameworks = "nice"),
  cybedtools_download_failed = function(cnd) message("No network: ", conditionMessage(cnd))
)
}
}
\seealso{
Other data loading:
\code{\link[=load_graph]{load_graph()}}
}
\concept{data loading}
