% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/similarity-helpers.R
\name{framework_similarity}
\alias{framework_similarity}
\title{Cross-framework unit-text similarity, top-n matches per unit}
\usage{
framework_similarity(rdf, from, to, n = 5)
}
\arguments{
\item{rdf}{An rdf object.}

\item{from, to}{Character scalars, the \code{framework_slug} values (from
\code{\link[=organizing_unit_framework_bindings]{organizing_unit_framework_bindings()}}) whose organizing units are
compared. May be identical, to find near-duplicate units within one
framework.}

\item{n}{Integer, matches to keep per from-unit (default \code{5}).}
}
\value{
A tibble with one row per (from unit, match): columns
\code{from_unit}, \code{to_unit} (both full IRIs), \code{score} (numeric in \verb{[0, 1]}),
\code{strength} (\code{"strong"}/\code{"moderate"}/\code{"weak"}/\code{"none"}), and \code{rank}
(integer, \verb{1..k}, dense per \code{from_unit}). A \code{from}-side unit with zero
candidates on the \code{to} side contributes no rows. A \code{from}-side unit
whose only candidates all score 0 still appears, ranked, with
\code{strength == "none"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Reproduces, as the one public entry point, the "full-document" Jaccard
similarity computed script-locally by the concordance data-prep scripts
(\verb{concordance/_data-prep-*.R}, e.g. \verb{_data-prep-nice-ecsf-alignment.R}'s
\code{build_full_doc()}): each organizing unit's comparison text is its own
\code{schema:name}, its own \code{cybed:elementText} when it carries one directly
(true for pedagogy units where the unit and its top-level statement share
one IRI, e.g. Cyber.org/CSTA cells), and the concatenated
\code{cybed:elementText} of every element reachable via
\code{\link[=unit_element_bindings]{unit_element_bindings()}} (true for workforce units, whose task/
knowledge/skill statements are separate elements linked by
\code{cybed:hasElement}). This single text-assembly rule is what lets one
function serve both structural shapes. Every (from-unit, to-unit) pair is
then scored with \code{jaccard()}, the top \code{n} matches per from-unit are kept
via \code{top_n_matches()}, and each score's \code{similarity_strength()} is
attached. The tokenizer, the Jaccard set-similarity, the stopword list
and the ranking tie-break are internal (\verb{@noRd}) and may change without
notice; only this function's signature and output shape are the
contract.

\code{from} and \code{to} select organizing units by \code{framework_slug}
(\code{\link[=organizing_unit_framework_bindings]{organizing_unit_framework_bindings()}}\verb{$framework_slug}), matching how
the concordance scripts partition units before scoring
(\code{framework_slug == "cyberorg-k12"} vs \code{"csta-2017"}, for example). A
unit with no name, no own text, and no child element text at all
contributes no rows on either side (empty document, nothing to compare).
}
\examples{
rdf <- make_demo_graph()
# make_demo_graph()'s two frameworks carry no cybed:elementText or
# cybed:hasElement text, so this is a zero-row tibble with the columns
# from_unit/to_unit/score/strength/rank.
framework_similarity(rdf, from = "demo-fw-a", to = "demo-fw-b")

\dontrun{
rdf <- load_combined_ntriples_graph()
framework_similarity(rdf, from = "cyberorg-k12", to = "csta-2017", n = 3)
}
}
\concept{similarity helpers}
