% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-invariants.R
\name{assert_graph_identity}
\alias{assert_graph_identity}
\title{Stop the build when an assembled graph fuses a unit with a statement}
\usage{
assert_graph_identity(rdf, examples = 3L)
}
\arguments{
\item{rdf}{An rdf object.}

\item{examples}{Integer, how many offending IRIs to name per framework.}
}
\value{
Invisibly \code{TRUE} when the graph is clean.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Calls \code{\link[=graph_identity_violations]{graph_identity_violations()}} and raises a classed condition when it
returns any row. The message names each affected framework, how many IRIs
it contributes, and the first few of them, so the offending framework is
identifiable without re-querying the graph.

The remedy is to declare a \code{unit_iri_prefix} for that framework in
\code{docs/framework-invariants.yml} and let the assembler mint its unit IRIs
with it. See \code{\link[=build_organizing_unit_node]{build_organizing_unit_node()}}.
}
\examples{
\dontrun{
assert_graph_identity(load_combined_ntriples_graph())
}
}
\seealso{
Other Graph invariants:
\code{\link[=assert_graph_invariants]{assert_graph_invariants()}},
\code{\link[=graph_identity_violations]{graph_identity_violations()}},
\code{\link[=graph_invariant_counts]{graph_invariant_counts()}}
}
\concept{Graph invariants}
