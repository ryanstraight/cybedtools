% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{framework_licenses}
\alias{framework_licenses}
\title{Licence facts for the package code and every framework}
\format{
A tibble with 15 rows and 10 columns.
\describe{
\item{layer}{Character. \code{"code"} for the package's own code, or
\code{"framework"} for a framework's source content. Exactly one \code{"code"}
row exists.}
\item{slug}{Character. \code{"cybedtools"} for the code row; otherwise the
framework slug, equal to \link{framework_summary}\verb{$framework_slug}.
Unique.}
\item{framework_name}{Character. Short display name, matching
\link{framework_summary}\verb{$framework_name} for framework rows.}
\item{license_short}{Character. A short honest label, under 40
characters. The value \link{framework_summary}\verb{$license} carries.}
\item{license}{Character. What the source's own document, licence file,
or published terms page says, verbatim or as a faithful close
quotation, naming the document or page it was read from. Where a
source states nothing, the row says so rather than filling the gap.}
\item{attribution}{Character. The attribution string to use, verbatim
where the steward prescribed one. \code{NA} where none is prescribed; the
package does not invent citation wording and attribute it to a
steward. This is the only column that may be \code{NA}.}
\item{public_redistribution}{Character. One of \code{"unrestricted"},
\code{"full_with_attribution"}, \code{"structure_only"}, or \code{"local_only"}, as
described above.}
\item{terms_url}{Character. A public URL for the terms or the source
document. Always \code{https}. Where a source publishes no
framework-specific terms page, this is the publisher's own page for
the document.}
\item{granted}{Logical. \code{TRUE} only where a steward gave cybedtools
specific written permission. An open licence anyone may rely on is
not a grant to cybedtools and is \code{FALSE}.}
\item{verified}{Date. When the terms were last read from the source.}
}
}
\source{
Each framework's own published document or terms page, cited in
the \code{license} and \code{terms_url} columns. See
\code{data-raw/build-framework-licenses.R}, \code{LICENSING.md}, and
\code{docs/framework-data-sources.md}.
}
\usage{
framework_licenses
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The single owner of licence facts in cybedtools. One row for the
package's own code (MIT) and one row per framework, built by
\code{data-raw/build-framework-licenses.R} from each source's own document,
licence file, or published terms page. \link{framework_summary}\verb{$license} is
derived from this tibble's \code{license_short} column, so the short label and
the detail can never disagree. Reach a single row with \code{\link[=cybed_license]{cybed_license()}}.

Nothing here is legal advice. The \code{license} column records what a source
says; deciding what that permits in your situation is your call, and for
several frameworks the honest answer is that the source says nothing and
the position is inferred.
}
\section{What the redistribution classes mean}{

\code{public_redistribution} is the package's own operating rule for what it
will publish from a framework, and it agrees by construction with
\code{docs/framework-invariants.yml}, where a framework carrying no
\code{public_redistribution} key means \code{"unrestricted"}.
\describe{
\item{\code{"unrestricted"}}{Structure and statement text may both be
published, subject to the source's own attribution and
non-commercial or share-alike terms where it has them. The class
speaks to redistribution, not to commercial reuse: CSTA and
Cyber.org K-12 are \code{"unrestricted"} here and still carry NC and
NC-SA obligations recorded in \code{license}.}
\item{\code{"full_with_attribution"}}{As above, with attribution the
steward asked for explicitly.}
\item{\code{"structure_only"}}{Titles, categories, levels, codes, counts
and mappings may be published. Statement text may not.}
\item{\code{"local_only"}}{Nothing beyond aggregate counts is published.
Analysis happens on the user's own machine, where no distribution
occurs.}
}
}

\examples{
framework_licenses
subset(framework_licenses, granted)
subset(framework_licenses, public_redistribution != "unrestricted",
       select = c("slug", "license_short", "public_redistribution"))
}
\seealso{
Other licensing:
\code{\link[=cybed_license]{cybed_license()}}
}
\concept{licensing}
\keyword{datasets}
