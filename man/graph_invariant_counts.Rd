% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-invariants.R
\name{graph_invariant_counts}
\alias{graph_invariant_counts}
\title{Measure the counts that \code{graph_invariants} declares}
\usage{
graph_invariant_counts(rdf)
}
\arguments{
\item{rdf}{An rdf object.}
}
\value{
A tibble with columns \code{scope} (\code{"combined"} or a framework's local
name), \code{measure} and \code{value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Computes, from an assembled graph, every quantity the \code{graph_invariants}
block of \code{docs/framework-invariants.yml} declares a band for: the combined
totals, and the per-framework element split into parents, sub-points and
examples. The strict-inflation ratio is derived here rather than declared,
so the file and the graph cannot disagree about how it is computed.
}
\examples{
\dontrun{
graph_invariant_counts(load_combined_ntriples_graph())
}
}
\seealso{
Other Graph invariants:
\code{\link[=assert_graph_identity]{assert_graph_identity()}},
\code{\link[=assert_graph_invariants]{assert_graph_invariants()}},
\code{\link[=graph_identity_violations]{graph_identity_violations()}}
}
\concept{Graph invariants}
