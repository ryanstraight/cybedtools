% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsonld-helpers.R
\name{build_example_node}
\alias{build_example_node}
\title{Construct a \code{cybed:Example} node}
\usage{
build_example_node(
  parent_element_id,
  ordinal,
  text,
  framework_prefix,
  framework_id
)
}
\arguments{
\item{parent_element_id}{Character, the framework-local id of the parent
element (without prefix), e.g., \code{"K-2.SEC.AUTH"}.}

\item{ordinal}{Integer, the 1-based example ordinal within the parent.}

\item{text}{Character, the example's text fragment.}

\item{framework_prefix}{Character, Tier 2 prefix.}

\item{framework_id}{Character, framework identifier for \code{cybed:partOf}.}
}
\value{
Named list (JSON-LD node) for the example.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

An Example is a pedagogical-scaffolding fragment lifted from a parent
element's "Clarification statement:" prose. Distinct from
\code{\link[=build_subpoint_node]{build_subpoint_node()}}, which represents enumerated sub-statements that
the framework specifies as part of a parent element's normative content.
Examples carry \verb{@type} \verb{[cybed:Example, cybed:RoleElement]} and no
framework-native subtype: Cyber.org K-12 and CSTA Clarification examples
are teacher-facing scaffolding rather than enumerable sub-standards, so
framework-native typing would overstate what the framework specifies.

Examples connect to their parent through the parent element's
\code{cybed:hasExample} predicate (parent → example direction). Examples do
not carry a back-pointer such as \code{cybed:elaborates}; the parent owns the
Example, not the converse. Examples are excluded from default
\code{cybed:hasElement} traversals: a role-level query for "all elements"
returns Subpoints but not Examples. Reach Examples by traversing the
parent element's \code{cybed:hasExample}.

\code{\link[=parse_subpoints]{parse_subpoints()}} flags rows from "Clarification statement:" sources as
\code{node_type == "Example"}; \code{\link[=expand_with_subpoints]{expand_with_subpoints()}} routes those rows to
this constructor and emits the parent's \code{cybed:hasExample} triples.
}
\examples{
ex <- build_example_node(
  parent_element_id = "K-2.SEC.AUTH",
  ordinal           = 1,
  text              = "not using common words as passwords",
  framework_prefix  = "cyberorg",
  framework_id      = "cyberorg-k12-v1.0"
)
ex[["@id"]]
ex[["@type"]]
}
\seealso{
Other Sub-point parsing:
\code{\link[=build_subpoint_node]{build_subpoint_node()}},
\code{\link[=expand_with_subpoints]{expand_with_subpoints()}},
\code{\link[=extend_role_element_ids]{extend_role_element_ids()}},
\code{\link[=parse_subpoints]{parse_subpoints()}}
}
\concept{Sub-point parsing}
