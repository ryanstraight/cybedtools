% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph-invariants.R
\name{graph_identity_violations}
\alias{graph_identity_violations}
\title{Find organizing units that are also statements, and self-referential links}
\usage{
graph_identity_violations(rdf)
}
\arguments{
\item{rdf}{An rdf object from \code{\link[rdflib:rdf_parse]{rdflib::rdf_parse()}} or
\code{\link[=load_combined_ntriples_graph]{load_combined_ntriples_graph()}}.}
}
\value{
A tibble with one row per violation and three columns: \code{check}
(\code{"unit_element_collision"} or \code{"has_element_self_loop"}), \code{iri}, and
\code{framework} (the local name of the framework the IRI declares with
\code{cybed:partOf}, or \code{"(no framework)"}). Zero rows when the graph is
clean.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Two graph-level identity checks, both run over an assembled graph:
\enumerate{
\item No IRI may be typed both \code{cybed:OrganizingUnit} and \code{cybed:RoleElement}.
An IRI that is both is one node standing for two different things, a
unit and a statement, because the framework numbers them out of one id
space.
\item No \code{cybed:hasElement} triple may have the same subject and object. A
unit that is its own element is the same defect seen from the edge
rather than from the node.
}

The report is data. \code{\link[=assert_graph_identity]{assert_graph_identity()}} is the gate.
}
\examples{
\dontrun{
rdf <- load_combined_ntriples_graph()
graph_identity_violations(rdf)
}
}
\seealso{
Other Graph invariants:
\code{\link[=assert_graph_identity]{assert_graph_identity()}},
\code{\link[=assert_graph_invariants]{assert_graph_invariants()}},
\code{\link[=graph_invariant_counts]{graph_invariant_counts()}}
}
\concept{Graph invariants}
